<nav class="flex flex-col">
    <section class="mx-auto p-5">
        <%= link_to(root_path) do %>
        <img src="https://reviews.mommiesdaily.com/_nuxt/img/logo.cc9023f.png" alt="Mommies Daily" />
        <% end %>
    </section>
    <section class="flex items-center lg:px-32 px-5">
        <div class="w-1/3">
            <ul class="flex items-center">
                <% @social_links.each do |item| %>
                <li class="h-8 w-8 flex items-center justify-center bg-gray-200 rounded-full mr-2">
                    <%= link_to(item[:link]) do %>
                    <i class="<%= item[:icon] %>"></i>
                    <% end %>
                </li>
                <% end %>
            </ul>
        </div>
        <div class="w-1/3">
            <ul class="flex items-center justify-center uppercase font-bold">
                <% @menus.each do |item| %>
                <li class="px-3 <%= item[:selected] ? "text-primary" : "" %>"><%= item[:title] %></li>
                <% end %>
            </ul>
        </div>
        <div class="w-1/3">
            <ul class="flex items-center justify-right">
            </li>
        </div>
    </section>
</nav>